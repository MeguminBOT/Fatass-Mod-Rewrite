function onCreate()
     --Script is empty because the script.lua located inside data/triple-trouble-fatass-metal-cover handles everything instead since that's more efficient here.
     close(true);
end